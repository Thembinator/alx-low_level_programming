#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 always success.
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("argc = %d\n", argc);
	return (0);
}
